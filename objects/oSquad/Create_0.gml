ds_list_add(oPlayer.squadObjectList, id);

squad = ds_list_create();

selected = false;

// Identification		Variables
objectName = "Squad";

// Health				Variables
maxHealth = 1;
health = maxHealth;

// Movement				Variables
moveSpeed = 1;
runSpeed = 1.8;